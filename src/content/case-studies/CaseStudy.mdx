---
# ── Case Study Template — "Selected Work" collection ─────────────────
# Copy this file to src/content/case-studies/<slug>.mdx and replace every
# <PLACEHOLDER> value. Keep the five H2 sections in order — the headings
# are mandatory and the collection's listing pages rely on them.
#
#   template: true  →  marks this file as a skeleton; set it to false (or
#                      delete the line) when publishing a real case study.
#   status: draft   →  flip to `finished` when the case study is published.
#
# Frontmatter reference (all validated by src/content.config.ts):
#   title         (string, required)  outcome-oriented headline
#   date          (date, required)    initial publication date, YYYY-MM-DD
#   timeframe     (string, required)  human-readable project window
#   summary       (string, required)  one-sentence card excerpt for the
#                                     homepage "Selected Work" grid
#   featured      (boolean)           surface on the homepage?
#   template      (boolean)           skeleton file — exclude from listings
#   status        (draft|finished)    document state
#   technologies  (string[])          core technologies / stack
#   instruments   (string[])          instrumentation used
#   repositories  ({label, href}[])   repo, docs, or deployment links
#   tags          (string[])          domain tags
# ─────────────────────────────────────────────────────────────────────

title: "<Outcome-oriented headline — what was built, not what it is>"
date: 2026-08-27
timeframe: "<YYYY – YYYY, or 'YYYY – present'>"
summary: "<One sentence: the problem, the build, the measured result.>"
featured: false
template: true
status: draft
technologies:
  - "<Core language / framework>"
  - "<Library>"
instruments:
  - "<LC-MS/MS, TWIMS, ICP-MS, GC-MS, ...>"
repositories:
  - label: "<GitHub>"
    href: "<https://github.com/...>"
    external: true
tags:
  - "<tag>"
---

{/* Intro — 2–3 sentences, no heading. State what was built, for whom, and why it matters. */}

## The Analytical Problem

{/* The analytical or scientific question that drove the work. What was hard, and why did existing tools / workflows fail to answer it? */}

## Context & Constraints

{/* The environment the work happened in: laboratory setting, team, and timeline. Then the constraints that shaped the solution — instrument heterogeneity, regulatory or QA/QC requirements, vendor lock-in, single-maintainer realities, data volumes, turnaround times. */}

## The Hardware/Chemistry Solution

{/* The instrumentation and chemistry decisions: what hardware was chosen, procured, or commissioned; what method chemistry solved the analytical problem; what had to happen at the bench before any software existed. */}

## The Computational Architecture

{/* The software side: pipeline stages, data formats, libraries, and the design decisions that make the workflow reproducible — config-as-code, versioned artifacts, testing, CI, open formats. Include a diagram or interactive island where it clarifies the architecture. */}

## Outcome & Impact

{/* What measurably changed: metrics, deliverables, downstream use, and what the next scientist or engineer inherits. End with where the work lives today (repositories / documentation). */}
